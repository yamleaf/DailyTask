# 版本更新日志

> 提交范围: `944561c1` → `b51ec25d` | [返回 README](README.md)

## 🐛 修复 (Bug Fixes)

### 初始化与稳定性

- **修复多处初始化与逻辑缺陷**

### 截屏相关

- **解决截屏资源初始化和图像获取问题** — 释放残留脏资源避免 `VirtualDisplay` 泄漏，修复
  `acquireLatestImage` 可能返回 null 的问题
- **避免后台排空旧帧导致截屏超时**
- **延长截屏等待时间至 10 秒**

### 任务调度

- **修复任务重置时状态标记时序问题**
- **保持任务完成后的运行状态**
- **任务完成时发送 Completed 事件**
- **保存启动的协程作业对象**

### 配置与类型

- **修正默认值使用和界面文字错误** — 统一使用预定义常量替代硬编码、修正"跳过休息日"为"跳过节假日"
  、修复服务状态 UI 同步逻辑
- **修正目标应用字段类型为整型**
- **修正默认配置和布局 ID 错误**

### 其他修复

- **修正悬浮窗初始位置至右侧垂直居中**
- **修正前台服务启动方式以支持数据同步类型**
- **修复通知保存函数中的日志冗余问题**
- **优化输入验证与键值保存逻辑** — 修正企业微信密钥保存位置、优化邮箱发送按钮输入校验、添加为空时抖动动画
- **指令需以 DT# 开头** — 避免普通消息误触发
- **将数据库操作切换至 IO 线程**

---

## 🔧 优化 (Improvements)

### EventBus → SharedFlow 架构重构

- **移除 EventBus 依赖，改用直接调用** — 全面移除 EventBus 库
- 截屏事件改用 `MutableSharedFlow` 实现，`CaptureImageService` 改为请求函数驱动
- 投影事件改用 `SharedFlow` 替换 EventBus
- 通知服务状态监听改用 `SharedFlow`
- 截屏结果处理改用 `SharedFlow`
- 更新通知逻辑改用 `SharedFlow`
- 事件通信改用 `SharedFlow`
- 监控回调接口改用 `SharedFlow`
- 任务停止改用 `TaskScheduler` 替换 EventBus
- 悬浮窗管理改用直接控制器
- 超时与截屏逻辑重构为回调驱动模式
- 通知监听事件改用回调替代 EventBus
- 假日数据更新改用回调替代 EventBus

### 任务调度重构

- **重构任务调度器** — 移除倒计时服务，改用协程实现
- **重构任务调度逻辑及队列管理**
- **重构为一次性计划链式调度**
- **拆分调度状态** — 独立运行状态与提示事件
- **移除任务运行状态持久化逻辑**

### 截屏与计时优化

- **重构截屏请求为同步等待模式**
- **重构截屏计时逻辑** — 移除 `CountDownTimer`，统一使用 `TimeoutTimerManager`
- **重构倒计时服务实现** — 使用 `Handler + SystemClock.elapsedRealtime()` 解决休眠漂移问题
- **优化截屏等待超时时间并格式化代码**

### 日期与节假日模块

- **移除旧节假日实现并重构调度逻辑**
- **移除节假日状态视图及相关代码**

### UI 与样式

- **优化消息渠道界面布局和交互逻辑** — 测试按钮移至标题栏右侧、移除单选按钮、简化操作流程
- **优化工作日管理布局**
- **优化目标应用选择校验与结果来源逻辑**
- **使用 Material3 文本样式替代硬编码属性**
- **更新命令布局背景样式**

### 代码清理

- **重构配置常量与消息渠道逻辑**
- **重构 `ExportDataModel`** — 优化属性顺序和命名、调整邮箱配置类型
- **重构 `TaskScheduler` 状态与 `MainActivity` 代码结构**
- **移除 `AlarmManager` 相关代码及组件**
- **移除未使用的任务时间差计算扩展**
- **移除主线程数据库查询**
- **统一消息分发逻辑为全局单例**

---

## ✨ 新增 (New Features)

### 核心功能

- **添加 DT# 远程指令前缀** — 避免普通聊天消息误触发任务指令
- **添加全部指令页面** — 支持查看所有可用指令
- **任务超时发送兜底截图** — 超时自动发送截图确保打卡完整性
- **超时后发送回主页信号** — 任务超时自动返回主页

### 工作日管理

- **新增中国节假日数据同步与配置存储管理**
- **新增节假日数据更新入口** (设置页面)
- **新增自定义工作日配置** — 支持用户灵活配置工作日
- **增加节假日自动跳过任务功能**
- **在主界面标题栏显示节假日状态** — 显示日期并附加节假日/补班/休息日状态

### 任务调度

- **支持任务自动跨天循环执行** — 任务可自动延续到次日，统一按钮状态

### 截屏增强

- **添加黑屏检测和重试机制** — 检测截屏是否大部分为黑色，最多重试 2 次

### 模块扩展

- **初始化 lite 模块并添加基础 UI 组件**
